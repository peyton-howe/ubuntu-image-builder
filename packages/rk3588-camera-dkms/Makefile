# Top-level out-of-tree Makefile for DKMS (M= pointing here).
# Descend into subdirs that have their own Makefile.

obj-m += imx708/

# Staged — uncomment together with dkms.conf entries:
# obj-m += rkisp2/
# obj-m += dcphy/
